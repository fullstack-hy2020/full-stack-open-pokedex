name: Hello World Workflow!

on:
  push:
    branches:
    - main

jobs:
  say_hello_world:
    runs-on: ubuntu-20.4
    steps:
      - name: Say Hello
        run: |
          echo "Hello World Workflow!"