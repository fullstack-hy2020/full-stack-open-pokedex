name: Deploy pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - master

jobs:
  setup:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '16'
      - run: npm install
      - run: npm run eslint
      - run: npm run test
        
