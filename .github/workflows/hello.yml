name: Hello World!

on:
  push:
    branches:
      - main

jobs:
  combined_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello
        run: echo "Hello World!"
      
      - name: now it is
        run: date

      - name: Directory content
        run: ls
