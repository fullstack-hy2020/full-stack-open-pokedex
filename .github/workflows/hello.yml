name: Hello World!

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say Hello
        run: |
          echo "Hello World!"
      - name: Print Date 
        run: |
          date
      - name: Print directory contents
        run: |
          ls -la 
